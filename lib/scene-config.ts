/** Artistic parameters, intentionally absent from the visitor interface. */
export const sceneConfig = {
  lensingStrength: 1.12,
  diskTilt: 1.39,
  rotationSpeed: 0.046,
  exposure: 1.22,
  starDensity: 0.62,
  bloom: 0.22,
  maxDpr: 1.5,
  mobileDpr: 1,
  targetFrameMs: 27,
  pointerParallax: 0.016,
};
